import { Typography } from '@mui/material';

const MoreText = () => {
  return (
      <Typography
        sx={{
          fontSize: '20px',
          textAlign: 'center',
          marginTop:'3rem'
        }}
      >
        すべて見る
      </Typography>
  );
}

export default MoreText;
